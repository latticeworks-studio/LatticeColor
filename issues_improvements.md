## Improvements
- [x] We need a way to create 16, 32, 64, 128 and 256 color palettes. Idea: you select a color your like then enter Palette mode; set a number of colors to create and it creates them with the root being the color you selected and making sure never to stray too far from the root color. After it's been generated, the user can edit each color manually and change its value through a picker. Then the user can save that palette internally and save a bunch of them this way. The user can also export a png of said palette. The png would be a 256x1 image so if it has a 128 color palette then the remaining 128px of the png image will be this color: `#C3C3C3` as empty means `#C3C3C3`